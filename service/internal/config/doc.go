/*
Package config handles the runtime configuration for the project.
Config variables are managed through Viper, and are passed in via environment variables. Logging is handled through Slog.
*/
package config
